---
const { title } = Astro.props
---

<section>
    { title && <h2>{title}</h2>}
    <slot/>
</section>

<style>
    section{
        max-width: 720px;
        margin: 0 auto 40px;
        line-height: 1.5;
        font-size: 1.5rem;
    }

    h2{
        margin-bottom: 8px;
        font-weight: 700;
        line-height: 1.5;
        font-size: 1.5rem;
    }
</style>